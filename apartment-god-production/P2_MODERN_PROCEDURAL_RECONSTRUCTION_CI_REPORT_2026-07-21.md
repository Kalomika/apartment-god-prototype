# P2 Modern Procedural Reconstruction CI Report

Generated: 2026-07-21T12:59:58Z
Branch start: a3c17b164bcbbee34810187553e3e19d2d646023
Overall status: FAIL

| Stage | Exit status |
|---|---:|
| Installer | 0 |
| Cache patch | 0 |
| Syntax | 0 |
| Repository check | 0 |
| Tests | 1 |
| Build | 99 |
| Built contract | 99 |

## Installer log
```text
```

## Cache log
```text
```

## Syntax log
```text
```

## Check log
```text

> apartment-god-prototype@0.3.0 check
> node scripts/check.js && node scripts/validate-sprite-pipeline.js --schema-only

Checked 101 source files
Sprite pipeline valid: 4 animation states, 13 sprite specs.
```

## Test log
```text
[31m+     }[39m
[31m+[39m
[31m+     recoverFrame(error, boot = false) {[39m
[31m+       if (this.runtimeFailed) return;[39m
[31m+       this.runtimeFailed = true;[39m
[31m+       this.frameErrorCount += 1;[39m
[31m+       console.error('[Apartment God] Phaser Migration 2 entered terminal recovery instead of blanking.', error);[39m
[31m+       clearBadRefreshState();[39m
[31m+[39m
[31m+       if (this.hiddenTicker) window.clearInterval(this.hiddenTicker);[39m
[31m+       this.hiddenTicker = null;[39m
[31m+       if (this.pointerHandler) this.input?.off?.('pointerdown', this.pointerHandler);[39m
[31m+       if (this.input) this.input.enabled = false;[39m
[31m+[39m
[31m+       if (this.state) {[39m
[31m+         try { sanitizeRuntimeState(this.state); } catch {}[39m
[31m+         for (const entity of this.state.entities || []) {[39m
[31m+           entity.path = [];[39m
[31m+           entity.poolRoute = null;[39m
[31m+           entity.target = null;[39m
[31m+           entity.pending = null;[39m
[31m+           entity.pose = 'stand';[39m
[31m+           entity.blockedT = 0;[39m
[31m+           entity.recoveryCount = 0;[39m
[31m+         }[39m
[31m+         this.state.saveStatus = { message: 'Phaser Migration 2 stopped safely after a runtime error' };[39m
[31m+         this.state.paused = true;[39m
[31m+       }[39m
[31m+[39m
[31m+       try { destroyNativeGameplayVisuals(this.nativeGameplayVisuals); } catch {}[39m
[31m+       this.nativeGameplayVisuals = null;[39m
[31m+       try { destroyReferenceCompletion(this); } catch {}[39m
[31m+       try { clearCharacterVisuals(this); } catch {}[39m
[31m+       try { this.children.removeAll(true); } catch {}[39m
[31m+[39m
[31m+       this.cameras?.main?.setBackgroundColor?.('#171a22');[39m
[31m+       this.add.rectangle(0, 0, PLAY_W, PLAY_H, 0x171a22).setOrigin(0, 0);[39m
[31m+       this.add.text(32, 64, boot ? 'Phaser Migration 2 boot recovery screen' : 'Phaser Migration 2 runtime recovery screen', {[39m
[31m+         fontFamily: 'system-ui', fontSize: 28, fontStyle: '900', color: '#f1c66a'[39m
[31m+       });[39m
[31m+       this.add.text(32, 108, 'The game stopped safely. The error screen will remain stable instead of looping or blanking.', {[39m
[31m+         fontFamily: 'system-ui', fontSize: 17, fontStyle: '700', color: '#f0f2f7', wordWrap: { width: PLAY_W - 64 }[39m
[31m+       });[39m
[31m+       this.add.text(32, 158, String(error?.message || error || 'Unknown error').slice(0, 220), {[39m
[31m+         fontFamily: 'system-ui', fontSize: 14, color: '#aab2c5', wordWrap: { width: PLAY_W - 64 }[39m
[31m+       });[39m
[31m+       this.registry.set('apartmentGodRuntimeFailed', true);[39m
[31m+       this.registry.set('apartmentGodRuntimeError', String(error?.message || error || 'Unknown error'));[39m
[31m+       this.scene.pause();[39m
[31m+     }[39m
[31m+   };[39m
[31m+ }[39m
[31m+[39m
[31m+ function runtimeLabel(assetFailures) {[39m
[31m+   const fallback = assetFailures.length ? ` | ${assetFailures.length} visual fallback${assetFailures.length === 1 ? '' : 's'}` : '';[39m
[31m+   return `PHASER MIGRATION 2 | native world | full gameplay | ${CHARACTER_ANIMATION_FPS} FPS${fallback}`;[39m
[31m+ }[39m
[31m+[39m
[31m+ function sanitizeRuntimeState(state) {[39m
[31m+   state.entities = Array.isArray(state.entities) ? state.entities : [];[39m
[31m+   state.speed = Number.isFinite(state.speed) && state.speed > 0 ? state.speed : 1;[39m
[31m+   state.time = Number.isFinite(state.time) ? state.time : 0;[39m
[31m+   state.floor = Number.isInteger(state.floor) ? state.floor : 0;[39m
[31m+   state.objectState ??= {};[39m
[31m+   state.roomLights ??= {};[39m
[31m+   state.tidiness ??= { rooms: {}, score: 100, activityMultiplier: 1.2 };[39m
[31m+   state.tidiness.rooms ??= {};[39m
[31m+   state.notifications = Array.isArray(state.notifications) ? state.notifications : [];[39m
[31m+   state.lifeControl ??= { mode: 'semi_auto', pendingChoices: [] };[39m
[31m+   state.lifeQuality ??= { lastMonthIndex: null, lastYearIndex: null, reviews: [], yearReviews: [] };[39m
[31m+   state.frontGate ??= { open: 0, requested: false };[39m
[31m+   applyMainFloorLayoutPolish(state);[39m
[31m+   applyRuntimeObjectCorrections();[39m
[31m+   for (const entity of state.entities) {[39m
[31m+     entity.path = Array.isArray(entity.path) ? entity.path : [];[39m
[31m+     entity.needs ??= {};[39m
[31m+     entity.skills ??= {};[39m
[31m+     entity.pose ||= 'stand';[39m
[31m+     entity.action ||= 'Idle';[39m
[31m+     entity.floor = Number.isInteger(entity.floor) ? entity.floor : 0;[39m
[31m+     entity.x = Number.isFinite(entity.x) ? entity.x : 120;[39m
[31m+     entity.y = Number.isFinite(entity.y) ? entity.y : 120;[39m
[31m+     cleanupStaleActorState(entity);[39m
[31m+   }[39m
[31m+   updateHouseTidiness(state);[39m
[31m+ }[39m
[31m+[39m
[31m+ function cleanupStaleActorState(entity) {[39m
[31m+   const hasPath = Array.isArray(entity.path) && entity.path.length > 0;[39m
[31m+   const hasPoolRoute = Array.isArray(entity.poolRoute?.points) && entity.poolRoute.points.length > 0;[39m
[31m+   const hasTarget = Boolean(entity.target || entity.pending);[39m
[31m+   const hasTimer = Number(entity.actionT || 0) > 0;[39m
[31m+   if (hasPath || hasPoolRoute || hasTarget || hasTimer || entity.hidden) return;[39m
[31m+   const action = String(entity.action || '').toLowerCase();[39m
[31m+   if (action === 'recovered' || action === 'runtime recovered' || action === 'walking' || action === 'running' || action.startsWith('going to ')) {[39m
[31m+     entity.action = 'Idle';[39m
[31m+     entity.pose = 'stand';[39m
[31m+     entity.blockedT = 0;[39m
[31m+     entity.recoveryCount = 0;[39m
[31m+   }[39m
[31m+ }[39m
[31m+[39m
[31m+ function runSimulationStep(state, dt) {[39m
[31m+   if (dt <= 0) return;[39m
[31m+   applyMainFloorLayoutPolish(state);[39m
[31m+   applyRuntimeObjectCorrections();[39m
[31m+   updateFrontYardEnvironment(state, dt);[39m
[31m+   requestGateForApproachingEntities(state, dt);[39m
[31m+   updateHouseTidiness(state);[39m
[31m+   updatePoolActivity(state, dt);[39m
[31m+   captureGateTraversalState(state);[39m
[31m+[39m
[31m+   for (const entity of state.entities) {[39m
[31m+     const poolChoreography = Array.isArray(entity.poolRoute?.points) || String(entity.action || '').toLowerCase().startsWith('pool:');[39m
[31m+     if (poolChoreography) continue;[39m
[31m+     const arrived = updateMovement(state, entity, dt);[39m
[31m+     if (arrived) resolveArrival(state, entity);[39m
[31m+   }[39m
[31m+[39m
[31m+   enforceGateTraversal(state);[39m
[31m+   updateActions(state, dt);[39m
[31m+   updateArcadeSystem(state, dt);[39m
[31m+   updateBasketballSystem(state, dt);[39m
[31m+   updateOffsiteHomeView(state);[39m
[31m+   updateCalendarRuntime(state);[39m
[31m+   updateAutoHooks(state, dt);[39m
[31m+   updateAutonomy(state, dt);[39m
[31m+   advanceGameClock(state, dt);[39m
[31m+   updateLifeQualitySystem(state);[39m
[31m+   updateHouseTidiness(state);[39m
[31m+ }[39m
[31m+[39m
[31m+ function advanceSimulation(state, rawDt) {[39m
[31m+   if (state.paused) return;[39m
[31m+   const maximumStep = document.hidden ? .2 : .05;[39m
[31m+   const maximumCatchup = document.hidden ? 4 : .2;[39m
[31m+   let remaining = Math.min(rawDt * state.speed, maximumCatchup);[39m
[31m+   let guard = 0;[39m
[31m+   while (remaining > .0001 && guard < 40) {[39m
[31m+     const step = Math.min(maximumStep, remaining);[39m
[31m+     runSimulationStep(state, step);[39m
[31m+     remaining -= step;[39m
[31m+     guard += 1;[39m
[31m+   }[39m
[31m+ }[39m
[31m+[39m
[31m+ function floorSignature(floorId) {[39m
[31m+   const floorRooms = floors.find(candidate => candidate.id === floorId)?.rooms || [];[39m
[31m+   const floorObjects = objects.filter(object => object.floor === floorId && !object.collisionOnly);[39m
[31m+   return `${floorId}|${floorRooms.map(room => `${room.id}:${room.x}:${room.y}:${room.w}:${room.h}`).join(';')}|${floorObjects.map(object => `${object.id}:${object.x}:${object.y}:${object.w}:${object.h}:${object.kind}:${object.hidden ? 1 : 0}`).join(';')}`;[39m
[31m+ }[39m
[31m+[39m
[31m+ function roomAlpha(roomId) {[39m
[31m+   if (roomId === 'yard' || roomId === 'front_yard' || roomId === 'garden') return .92;[39m
[31m+   if (roomId.includes('pool')) return .9;[39m
[31m+   if (roomId.includes('garage') || roomId.includes('driveway') || roomId.includes('road')) return .9;[39m
[31m+   return .97;[39m
[31m+ }[39m
[31m+[39m
[31m+ function parseHexColor(value, fallback) {[39m
[31m+   if (typeof value !== 'string') return fallback;[39m
[31m+   const normalized = value.replace('#', '');[39m
[31m+   const parsed = Number.parseInt(normalized, 16);[39m
[31m+   return Number.isFinite(parsed) ? parsed : fallback;[39m
[31m+ }[39m
[31m+[39m
[31m+ function loadRefreshStateSafely(state) {[39m
[31m+   try {[39m
[31m+     const snapshot = loadRefreshState(state);[39m
[31m+     if (snapshot?.floor != null) state.floor = snapshot.floor;[39m
[31m+     return snapshot;[39m
[31m+   } catch (error) {[39m
[31m+     clearBadRefreshState();[39m
[31m+     console.warn('[Apartment God] Ignored incompatible refresh state.', error);[39m
[31m+     return null;[39m
[31m+   }[39m
[31m+ }[39m
[31m+[39m
[31m+ function clearBadRefreshState() {[39m
[31m+   try { localStorage.removeItem(REFRESH_SAVE_KEY); } catch {}[39m
[31m+ }[39m
[31m+[39m
[31m+ function drawHardBootError(canvas, error) {[39m
[31m+   const context = canvas.getContext('2d');[39m
[31m+   if (!context) return;[39m
[31m+   canvas.width = PLAY_W;[39m
[31m+   canvas.height = PLAY_H;[39m
[31m+   context.fillStyle = '#171a22';[39m
[31m+   context.fillRect(0, 0, PLAY_W, PLAY_H);[39m
[31m+   context.fillStyle = '#f1c66a';[39m
[31m+   context.font = '900 34px system-ui';[39m
[31m+   context.fillText('Phaser Migration 2 could not boot', 42, 84);[39m
[31m+   context.fillStyle = '#f0f2f7';[39m
[31m+   context.font = '700 18px system-ui';[39m
[31m+   context.fillText('The game stopped on a visible error screen instead of showing a blank canvas.', 42, 122);[39m
[31m+   context.fillStyle = '#aab2c5';[39m
[31m+   context.font = '600 14px system-ui';[39m
[31m+   context.fillText(String(error?.message || error || 'Unknown error').slice(0, 150), 42, 160);[39m
[31m+ }[39m
[31m+[39m

[36m [2m❯[22m tests/phaser-migration-2-reference-visuals.test.js:[2m33:27[22m[39m
    [90m 31| [39m  [34mit[39m([32m'keeps the native Phaser runtime and removes the old broad visual[39m…
    [90m 32| [39m    [34mexpect[39m(runtimeSource)[33m.[39m[34mtoContain[39m([32m'phaserMigration2VisualCatalog'[39m)[33m;[39m
    [90m 33| [39m    [34mexpect[39m(runtimeSource)[33m.[39m[34mtoContain[39m([32m'textureForRoom(room)'[39m)[33m;[39m
    [90m   | [39m                          [31m^[39m
    [90m 34| [39m    [34mexpect[39m(runtimeSource)[33m.[39mnot[33m.[39m[34mtoContain[39m([32m'drawPhaserEnvironment'[39m)[33m;[39m
    [90m 35| [39m    [34mexpect[39m(runtimeSource)[33m.[39mnot[33m.[39m[34mtoContain[39m([32m'textures.addCanvas'[39m)[33m;[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯[22m[39m

[2m Test Files [22m [1m[31m2 failed[39m[22m[2m | [22m[1m[32m20 passed[39m[22m[90m (22)[39m
[2m      Tests [22m [1m[31m2 failed[39m[22m[2m | [22m[1m[32m82 passed[39m[22m[90m (84)[39m
[2m   Start at [22m 12:59:55
[2m   Duration [22m 2.40s[2m (transform 387ms, setup 0ms, collect 1.05s, tests 208ms, environment 8ms, prepare 1.72s)[22m


::error file=/home/runner/work/apartment-god-prototype/apartment-god-prototype/tests/phaser-migration-2-perfecting-runtime.test.js,title=tests/phaser-migration-2-perfecting-runtime.test.js > Phaser Migration 2 perfection runtime corrections > falls back per optional visual instead of failing the whole scene,line=62,column=27::AssertionError: expected 'import { PLAY_H, PLAY_W } from \'./co…' to contain '\'__MISSING\''%0A%0A- Expected%0A+ Received%0A%0A- '__MISSING'%0A+ import { PLAY_H, PLAY_W } from './config.js';%0A+ import { createState, selected } from './state.js';%0A+ import { floors, objects } from './world.js';%0A+ import { createUi } from './ui.js';%0A+ import { updateMovement } from './movement.js';%0A+ import { resolveArrival, updateActions } from './actions.js';%0A+ import { updateAutoHooks } from './autoHooks.js';%0A+ import { updateAutonomy } from './autonomy.js';%0A+ import { updateCalendarRuntime } from './calendarRuntime.js';%0A+ import { updateLifeQualitySystem } from './lifeQualitySystem.js';%0A+ import { installCameraSwipeNavigation, updateCameraTransition } from './cameraNavigation.js';%0A+ import { loadRefreshState, saveRefreshState, updateRefreshAutosave } from './saveSystem.js';%0A+ import { applyRuntimeRegressionGuards } from './runtimeRegressionGuards.js';%0A+ import { updatePoolActivity } from './poolActivitySystem.js';%0A+ import { updateHouseTidiness } from './tidinessSystem.js';%0A+ import { formatTime } from './renderHelpers.js';%0A+ import { advanceGameClock } from './timeSystem.js';%0A+ import { installFrontYardWorld, updateFrontYardEnvironment } from './frontYardDriveway.js';%0A+ import { applyRuntimeObjectCorrections } from './runtimeObjectCorrections.js';%0A+ import { applyMainFloorLayoutPolish } from './mainFloorLayoutPolish.js';%0A+ import { updateArcadeSystem } from './arcadeSystem.js';%0A+ import { updateBasketballSystem } from './basketballSystem.js';%0A+ import { updateOffsiteHomeView } from './offsiteOverlay.js';%0A+ import { captureGateTraversalState, enforceGateTraversal, requestGateForApproachingEntities } from './gateTraversalGuard.js';%0A+ import {%0A+   CHARACTER_ANIMATION_FPS,%0A+   clearCharacterVisuals,%0A+   registerCharacterAnimations,%0A+   syncCharacterVisuals%0A+ } from './phaserCharacterAnimationSystem.js';%0A+ import {%0A+   createNativeGameplayVisuals,%0A+   destroyNativeGameplayVisuals,%0A+   syncNativeGameplayVisuals%0A+ } from './phaserMigration2GameplayVisuals.js';%0A+ import {%0A+   PM2_CHARACTER_SHEETS,%0A+   PM2_OBJECT_TEXTURES,%0A+   PM2_ROOM_TEXTURES%0A+ } from './phaserMigration2VisualCatalog.js';%0A+ import {%0A+   createModernProceduralObject,%0A+   createModernProceduralRoom,%0A+   isModernProceduralVisual,%0A+   refreshModernProceduralObject%0A+ } from './phaserMigration2ModernProceduralRenderer.js';%0A+ import {%0A+   preloadReferenceCompletion,%0A+   createReferenceCompletion,%0A+   syncReferenceCompletion,%0A+   destroyReferenceCompletion%0A+ } from './phaserMigration2ReferenceCompletion.js';%0A+%0A+ installFrontYardWorld();%0A+ applyMainFloorLayoutPolish();%0A+ applyRuntimeObjectCorrections();%0A+%0A+ const REFRESH_SAVE_KEY = 'apartment_god_test_refresh_state_v3';%0A+%0A+ export async function bootPhaserMigration2Game() {%0A+   const canvas = document.getElementById('game');%0A+   if (!canvas) return null;%0A+   try {%0A+     const phaserModule = await import('../vendor/phaser.esm.js').catch(() => import('https://cdn.jsdelivr.net/npm/phaser@3.90.0/dist/phaser.esm.js'));%0A+     const Phaser = phaserModule.default || phaserModule;%0A+     const SceneClass = createApartmentGodNativeScene(Phaser);%0A+     return new Phaser.Game({%0A+       type: Phaser.CANVAS,%0A+       canvas,%0A+       width: PLAY_W,%0A+       height: PLAY_H,%0A+       backgroundColor: '#101722',%0A+       render: { antialias: true, pixelArt: false, transparent: false, clearBeforeRender: true },%0A+       scale: { mode: Phaser.Scale.NONE, autoCenter: Phaser.Scale.NO_CENTER, width: PLAY_W, height: PLAY_H },%0A+       scene: [SceneClass]%0A+     });%0A+   } catch (error) {%0A+     drawHardBootError(canvas, error);%0A+     console.error('[Apartment God] Phaser Migration 2 failed before scene creation.', error);%0A+     return null;%0A+   }%0A+ }%0A+%0A+ function createApartmentGodNativeScene(Phaser) {%0A+   return class ApartmentGodNativeScene extends Phaser.Scene {%0A+     constructor() {%0A+       super('ApartmentGodNativeScene');%0A+       this.currentFloor = null;%0A+       this.floorSignature = '';%0A+       this.frameErrorCount = 0;%0A+       this.runtimeFailed = false;%0A+       this.lastHiddenTick = 0;%0A+       this.hiddenTicker = null;%0A+       this.nativeObjects = [];%0A+       this.pm2ActorVisuals = new Map();%0A+       this.assetFailures = [];%0A+       this.nativeGameplayVisuals = null;%0A+       this.beforeUnloadHandler = null;%0A+       this.visibilityHandler = null;%0A+       this.pointerHandler = null;%0A+     }%0A+%0A+     preload() {%0A+       preloadReferenceCompletion(this);%0A+       this.loadText = this.add.text(28, 28, 'Apartment God Phaser Migration 2 loading full gameplay...', {%0A+         fontFamily: 'system-ui', fontSize: 20, fontStyle: '900', color: '#f1c66a'%0A+       }).setDepth(10000);%0A+%0A+       for (const [key, url] of Object.entries(PM2_ROOM_TEXTURES)) this.load.svg(`pm2-room-${key}`, url, { width: 128, height: 128 });%0A+       for (const [key, url] of Object.entries(PM2_OBJECT_TEXTURES)) this.load.svg(`pm2-object-${key}`, url, { width: 128, height: 128 });%0A+       for (const [key, url] of Object.entries(PM2_CHARACTER_SHEETS)) this.load.svg(`pm2-${key}`, url, { width: 512, height: 512 });%0A+       this.load.on('loaderror', file => {%0A+         const key = file?.key || file?.src || 'unknown asset';%0A+         if (!this.assetFailures.includes(key)) this.assetFailures.push(key);%0A+         console.warn('[Apartment God] Phaser Migration 2 visual asset failed safely:', file?.key, file?.src);%0A+       });%0A+     }%0A+%0A+     create() {%0A+       try {%0A+         this.state = createState();%0A+         this.state.runtimeRenderer = 'phaser-migration-2-native-full-gameplay';%0A+         this.state.assetFailures = [...this.assetFailures];%0A+         loadRefreshStateSafely(this.state);%0A+         sanitizeRuntimeState(this.state);%0A+         applyRuntimeRegressionGuards(this.state);%0A+%0A+         this.roomLayer = this.add.container(0, 0).setDepth(0);%0A+         this.objectLayer = this.add.container(0, 0).setDepth(20);%0A+         this.actorLayer = this.add.container(0, 0).setDepth(60);%0A+         this.fxLayer = this.add.container(0, 0).setDepth(90);%0A+         this.poolGraphics = this.add.graphics();%0A+         this.fxLayer.add(this.poolGraphics);%0A+         this.nativeGameplayVisuals = createNativeGameplayVisuals(this, this.fxLayer);%0A+         createReferenceCompletion(this);%0A+         registerCharacterAnimations(this);%0A+%0A+         this.statusText = this.add.text(12, 12, '', {%0A+           fontFamily: 'system-ui', fontSize: 14, fontStyle: '900', color: '#f8fbff', backgroundColor: 'rgba(7,10,16,.55)', padding: { x: 8, y: 5 }%0A+         }).setDepth(1000);%0A+         this.runtimeText = this.add.text(12, PLAY_H - 25, runtimeLabel(this.assetFailures), {%0A+           fontFamily: 'system-ui', fontSize: 11, fontStyle: '900', color: '#f1c66a', backgroundColor: 'rgba(7,10,16,.45)', padding: { x: 7, y: 3 }%0A+         }).setDepth(1000);%0A+%0A+         installCameraSwipeNavigation(this.state, this.game.canvas);%0A+         this.ui = createUi(this.state, this.game.canvas, { externalInput: true });%0A+         this.pointerHandler = pointer => this.handleGamePointer(pointer);%0A+         this.input.on('pointerdown', this.pointerHandler);%0A+%0A+         this.beforeUnloadHandler = () => {%0A+           if (!this.runtimeFailed && this.state) saveRefreshState(this.state);%0A+         };%0A+         this.visibilityHandler = () => {%0A+           if (!this.state || this.runtimeFailed) return;%0A+           this.state.backgroundMode = document.hidden;%0A+           if (!document.hidden) this.state.saveStatus = { message: 'Returned from background' };%0A+         };%0A+         window.addEventListener('beforeunload', this.beforeUnloadHandler);%0A+         document.addEventListener('visibilitychange', this.visibilityHandler);%0A+%0A+         this.lastHiddenTick = performance.now();%0A+         this.hiddenTicker = window.setInterval(() => this.hiddenTick(), 1000);%0A+         this.events.once('shutdown', () => this.shutdownRuntime());%0A+         this.events.once('destroy', () => this.shutdownRuntime());%0A+%0A+         this.registry.set('apartmentGodRuntime', 'phaser-migration-2-native-full-main-gameplay');%0A+         this.registry.set('apartmentGodCharacterFps', CHARACTER_ANIMATION_FPS);%0A+         this.registry.set('apartmentGodAssetFallbackCount', this.assetFailures.length);%0A+         if (this.assetFailures.length) this.state.saveStatus = { message: `${this.assetFailures.length} visual asset fallback(s) active` };%0A+         this.loadText?.destroy();%0A+         this.renderNativeFrame();%0A+         this.ui?.renderHud();%0A+       } catch (error) {%0A+         this.recoverFrame(error, true);%0A+       }%0A+     }%0A+%0A+     update(_time, deltaMs) {%0A+       if (this.runtimeFailed || !this.state) return;%0A+       try {%0A+         sanitizeRuntimeState(this.state);%0A+         applyRuntimeRegressionGuards(this.state);%0A+         const rawElapsed = Math.max(0, deltaMs / 1000);%0A+         const rawDt = Math.min(document.hidden ? 4 : .2, rawElapsed);%0A+         const cameraDt = Math.min(.05, rawElapsed);%0A+         updateCameraTransition(this.state, cameraDt);%0A+         if (!document.hidden) advanceSimulation(this.state, rawDt);%0A+         updateRefreshAutosave(this.state, cameraDt);%0A+         if (this.state.skipRecap?.visibleT > 0) this.state.skipRecap.visibleT -= cameraDt;%0A+         this.renderNativeFrame();%0A+         this.ui?.renderHud();%0A+       } catch (error) {%0A+         this.recoverFrame(error, false);%0A+       }%0A+     }%0A+%0A+     handleGamePointer(pointer) {%0A+       if (this.runtimeFailed || !this.ui?.handleCanvasPoint) return;%0A+       const bounds = this.game.canvas.getBoundingClientRect();%0A+       const event = pointer.event;%0A+       const menuX = event?.clientX == null ? pointer.x : event.clientX - bounds.left;%0A+       const menuY = event?.clientY == null ? pointer.y : event.clientY - bounds.top;%0A+       this.ui.handleCanvasPoint(pointer.x, pointer.y, menuX, menuY);%0A+     }%0A+%0A+     hiddenTick() {%0A+       if (this.runtimeFailed || !this.state || !document.hidden || this.state.paused) return;%0A+       const now = performance.now();%0A+       const elapsed = Math.min(4, Math.max(0, (now - this.lastHiddenTick) / 1000));%0A+       this.lastHiddenTick = now;%0A+       try {%0A+         sanitizeRuntimeState(this.state);%0A+         applyRuntimeRegressionGuards(this.state);%0A+         advanceSimulation(this.state, elapsed);%0A+         updateRefreshAutosave(this.state, Math.min(1, elapsed));%0A+       } catch (error) {%0A+         this.recoverFrame(error, false);%0A+       }%0A+     }%0A+%0A+     renderNativeFrame() {%0A+       if (this.runtimeFailed || !this.state) return;%0A+       const signature = floorSignature(this.state.floor);%0A+       if (this.currentFloor !== this.state.floor || this.floorSignature !== signature) this.rebuildFloor(signature);%0A+       this.refreshObjectStates();%0A+       syncCharacterVisuals(this, this.state, this.actorLayer);%0A+       this.refreshPoolFx();%0A+       syncReferenceCompletion(this);%0A+       syncNativeGameplayVisuals(this, this.state, this.nativeGameplayVisuals);%0A+       const actor = selected(this.state);%0A+       const tidy = Number.isFinite(this.state.tidiness?.score) ? ` tidy ${Math.round(this.state.tidiness.score)}%25` : '';%0A+       const floor = floors.find(candidate => candidate.id === this.state.floor);%0A+       this.statusText?.setText(`${formatTime(this.state.time)}   $${Math.round(this.state.money ?? 0)}   ${this.state.autonomyMode}${tidy}   ${floor?.name || this.state.floor}   ${actor?.name || ''}: ${actor?.action || 'Idle'}`);%0A+       this.statusText?.setVisible(true);%0A+       this.runtimeText?.setVisible(true);%0A+     }%0A+%0A+     rebuildFloor(signature = floorSignature(this.state.floor)) {%0A+       if (this.runtimeFailed || !this.roomLayer || !this.objectLayer) return;%0A+       this.currentFloor = this.state.floor;%0A+       this.floorSignature = signature;%0A+       this.roomLayer.removeAll(true);%0A+       this.objectLayer.removeAll(true);%0A+       this.nativeObjects = [];%0A+       const floor = floors.find(candidate => candidate.id === this.state.floor);%0A+       if (!floor) return;%0A+%0A+       for (const room of floor.rooms || []) {%0A+         const panel = createModernProceduralRoom(this, room, floor.id, this.state);%0A+         this.roomLayer.add(panel);%0A+         const label = this.add.text(room.x + 16, room.y + 14, room.name, {%0A+           fontFamily: 'system-ui', fontSize: 11, fontStyle: '900', color: '#3f352d', backgroundColor: 'rgba(247,242,233,.72)', padding: { x: 5, y: 3 }%0A+         });%0A+         label.setVisible(Boolean(this.state.debugVisualLabels));%0A+         label.setDepth(room.y - 850);%0A+         this.roomLayer.add(label);%0A+       }%0A+%0A+       const floorObjects = objects%0A+         .filter(candidate => candidate.floor === this.state.floor)%0A+         .sort((a, b) => (a.y + a.h) - (b.y + b.h));%0A+       for (const object of floorObjects) {%0A+         const visual = createModernProceduralObject(this, object, this.state);%0A+         this.objectLayer.add(visual);%0A+         this.nativeObjects.push(visual);%0A+       }%0A+       this.refreshObjectStates();%0A+     }%0A+%0A+     refreshObjectStates() {%0A+       if (this.runtimeFailed) return;%0A+       for (const sprite of this.nativeObjects) {%0A+         const object = sprite.pm2Object;%0A+         if (!object) continue;%0A+         const visible = object.floor === this.state.floor && !object.hidden && !object.collisionOnly;%0A+         sprite.setVisible(visible);%0A+         if (!visible) continue;%0A+         if (isModernProceduralVisual(sprite)) {%0A+           sprite.setPosition(object.x + object.w / 2, object.y + object.h / 2);%0A+           sprite.setDepth(object.y + object.h);%0A+           sprite.setRotation(0);%0A+           sprite.setAlpha(1);%0A+           refreshModernProceduralObject(sprite, this.state);%0A+           continue;%0A+         }%0A+         sprite.setPosition(object.x + object.w / 2, object.y + object.h / 2);%0A+         sprite.setDisplaySize(Math.max(18, object.w), Math.max(18, object.h));%0A+         sprite.setDepth(object.y + object.h);%0A+         sprite.setRotation(Number(object.renderAngle || 0));%0A+         sprite.clearTint();%0A+         sprite.setAlpha(object.kind === 'light' ? .5 : 1);%0A+%0A+         if (object.kind === 'tv') sprite.setTint(this.state.tv?.on ? 0x74e6ff : 0x5d6876);%0A+         if (['shower','bathtub','dog_bath'].includes(object.kind)) {%0A+           const active = this.state.entities?.some(entity => entity.showerObjectId === object.id && entity.actionT > 0)%0A+             || this.state.entities?.some(entity => entity.currentActionId === 'wash_dog' && entity.actionT > 0 && entity.floor === object.floor);%0A+           if (active) sprite.setTint(0x9feaff);%0A+         }%0A+         if (object.kind === 'toilet') {%0A+           const active = this.state.entities?.some(entity => entity.toiletObjectId === object.id && entity.actionT > 0);%0A+           if (active) sprite.setTint(0xf1c66a);%0A+         }%0A+         if (object.kind === 'basketball_court') sprite.setAlpha(.32);%0A+         if (object.id?.includes('garage_door') || String(object.label || '').toLowerCase().includes('garage door')) {%0A+           sprite.setAlpha(this.state.objectState?.garageDoorOpen ? .35 : 1);%0A+           if (this.state.objectState?.garageDoorOpen) sprite.setTint(0x74e6ff);%0A+         }%0A+       }%0A+     }%0A+%0A+     refreshPoolFx() {%0A+       const graphics = this.poolGraphics;%0A+       if (!graphics || this.runtimeFailed) return;%0A+       graphics.clear();%0A+       const game = this.state.poolGame;%0A+       if (!game || game.floor !== this.state.floor || !Array.isArray(game.balls)) return;%0A+%0A+       if (game.cueLine?.t > 0) {%0A+         graphics.lineStyle(2, 0xf1c66a, Math.min(1, game.cueLine.t));%0A+         graphics.lineBetween(game.cueLine.x1, game.cueLine.y1, game.cueLine.x2, game.cueLine.y2);%0A+       }%0A+       if (game.cueThrust?.t > 0) {%0A+         graphics.lineStyle(4, 0xc99d62, Math.min(1, game.cueThrust.t * 2));%0A+         graphics.lineBetween(game.cueThrust.x1, game.cueThrust.y1, game.cueThrust.x2, game.cueThrust.y2);%0A+       }%0A+       for (const ball of game.balls) {%0A+         if (ball.pocketed) continue;%0A+         graphics.fillStyle(parseHexColor(ball.fill, 0xf8fbff), 1);%0A+         graphics.fillCircle(ball.x, ball.y, 7);%0A+         graphics.lineStyle(1.5, 0x111820, 1);%0A+         graphics.strokeCircle(ball.x, ball.y, 7);%0A+       }%0A+     }%0A+%0A+     shutdownRuntime() {%0A+       if (this.hiddenTicker) window.clearInterval(this.hiddenTicker);%0A+       this.hiddenTicker = null;%0A+       if (this.beforeUnloadHandler) window.removeEventListener('beforeunload', this.beforeUnloadHandler);%0A+       if (this.visibilityHandler) document.removeEventListener('visibilitychange', this.visibilityHandler);%0A+       if (this.pointerHandler) this.input?.off?.('pointerdown', this.pointerHandler);%0A+       this.beforeUnloadHandler = null;%0A+       this.visibilityHandler = null;%0A+       this.pointerHandler = null;%0A+       destroyNativeGameplayVisuals(this.nativeGameplayVisuals);%0A+       this.nativeGameplayVisuals = null;%0A+       destroyReferenceCompletion(this);%0A+       clearCharacterVisuals(this);%0A+     }%0A+%0A+     recoverFrame(error, boot = false) {%0A+       if (this.runtimeFailed) return;%0A+       this.runtimeFailed = true;%0A+       this.frameErrorCount += 1;%0A+       console.error('[Apartment God] Phaser Migration 2 entered terminal recovery instead of blanking.', error);%0A+       clearBadRefreshState();%0A+%0A+       if (this.hiddenTicker) window.clearInterval(this.hiddenTicker);%0A+       this.hiddenTicker = null;%0A+       if (this.pointerHandler) this.input?.off?.('pointerdown', this.pointerHandler);%0A+       if (this.input) this.input.enabled = false;%0A+%0A+       if (this.state) {%0A+         try { sanitizeRuntimeState(this.state); } catch {}%0A+         for (const entity of this.state.entities || []) {%0A+           entity.path = [];%0A+           entity.poolRoute = null;%0A+           entity.target = null;%0A+           entity.pending = null;%0A+           entity.pose = 'stand';%0A+           entity.blockedT = 0;%0A+           entity.recoveryCount = 0;%0A+         }%0A+         this.state.saveStatus = { message: 'Phaser Migration 2 stopped safely after a runtime error' };%0A+         this.state.paused = true;%0A+       }%0A+%0A+       try { destroyNativeGameplayVisuals(this.nativeGameplayVisuals); } catch {}%0A+       this.nativeGameplayVisuals = null;%0A+       try { destroyReferenceCompletion(this); } catch {}%0A+       try { clearCharacterVisuals(this); } catch {}%0A+       try { this.children.removeAll(true); } catch {}%0A+%0A+       this.cameras?.main?.setBackgroundColor?.('#171a22');%0A+       this.add.rectangle(0, 0, PLAY_W, PLAY_H, 0x171a22).setOrigin(0, 0);%0A+       this.add.text(32, 64, boot ? 'Phaser Migration 2 boot recovery screen' : 'Phaser Migration 2 runtime recovery screen', {%0A+         fontFamily: 'system-ui', fontSize: 28, fontStyle: '900', color: '#f1c66a'%0A+       });%0A+       this.add.text(32, 108, 'The game stopped safely. The error screen will remain stable instead of looping or blanking.', {%0A+         fontFamily: 'system-ui', fontSize: 17, fontStyle: '700', color: '#f0f2f7', wordWrap: { width: PLAY_W - 64 }%0A+       });%0A+       this.add.text(32, 158, String(error?.message || error || 'Unknown error').slice(0, 220), {%0A+         fontFamily: 'system-ui', fontSize: 14, color: '#aab2c5', wordWrap: { width: PLAY_W - 64 }%0A+       });%0A+       this.registry.set('apartmentGodRuntimeFailed', true);%0A+       this.registry.set('apartmentGodRuntimeError', String(error?.message || error || 'Unknown error'));%0A+       this.scene.pause();%0A+     }%0A+   };%0A+ }%0A+%0A+ function runtimeLabel(assetFailures) {%0A+   const fallback = assetFailures.length ? ` | ${assetFailures.length} visual fallback${assetFailures.length === 1 ? '' : 's'}` : '';%0A+   return `PHASER MIGRATION 2 | native world | full gameplay | ${CHARACTER_ANIMATION_FPS} FPS${fallback}`;%0A+ }%0A+%0A+ function sanitizeRuntimeState(state) {%0A+   state.entities = Array.isArray(state.entities) ? state.entities : [];%0A+   state.speed = Number.isFinite(state.speed) && state.speed > 0 ? state.speed : 1;%0A+   state.time = Number.isFinite(state.time) ? state.time : 0;%0A+   state.floor = Number.isInteger(state.floor) ? state.floor : 0;%0A+   state.objectState ??= {};%0A+   state.roomLights ??= {};%0A+   state.tidiness ??= { rooms: {}, score: 100, activityMultiplier: 1.2 };%0A+   state.tidiness.rooms ??= {};%0A+   state.notifications = Array.isArray(state.notifications) ? state.notifications : [];%0A+   state.lifeControl ??= { mode: 'semi_auto', pendingChoices: [] };%0A+   state.lifeQuality ??= { lastMonthIndex: null, lastYearIndex: null, reviews: [], yearReviews: [] };%0A+   state.frontGate ??= { open: 0, requested: false };%0A+   applyMainFloorLayoutPolish(state);%0A+   applyRuntimeObjectCorrections();%0A+   for (const entity of state.entities) {%0A+     entity.path = Array.isArray(entity.path) ? entity.path : [];%0A+     entity.needs ??= {};%0A+     entity.skills ??= {};%0A+     entity.pose ||= 'stand';%0A+     entity.action ||= 'Idle';%0A+     entity.floor = Number.isInteger(entity.floor) ? entity.floor : 0;%0A+     entity.x = Number.isFinite(entity.x) ? entity.x : 120;%0A+     entity.y = Number.isFinite(entity.y) ? entity.y : 120;%0A+     cleanupStaleActorState(entity);%0A+   }%0A+   updateHouseTidiness(state);%0A+ }%0A+%0A+ function cleanupStaleActorState(entity) {%0A+   const hasPath = Array.isArray(entity.path) && entity.path.length > 0;%0A+   const hasPoolRoute = Array.isArray(entity.poolRoute?.points) && entity.poolRoute.points.length > 0;%0A+   const hasTarget = Boolean(entity.target || entity.pending);%0A+   const hasTimer = Number(entity.actionT || 0) > 0;%0A+   if (hasPath || hasPoolRoute || hasTarget || hasTimer || entity.hidden) return;%0A+   const action = String(entity.action || '').toLowerCase();%0A+   if (action === 'recovered' || action === 'runtime recovered' || action === 'walking' || action === 'running' || action.startsWith('going to ')) {%0A+     entity.action = 'Idle';%0A+     entity.pose = 'stand';%0A+     entity.blockedT = 0;%0A+     entity.recoveryCount = 0;%0A+   }%0A+ }%0A+%0A+ function runSimulationStep(state, dt) {%0A+   if (dt <= 0) return;%0A+   applyMainFloorLayoutPolish(state);%0A+   applyRuntimeObjectCorrections();%0A+   updateFrontYardEnvironment(state, dt);%0A+   requestGateForApproachingEntities(state, dt);%0A+   updateHouseTidiness(state);%0A+   updatePoolActivity(state, dt);%0A+   captureGateTraversalState(state);%0A+%0A+   for (const entity of state.entities) {%0A+     const poolChoreography = Array.isArray(entity.poolRoute?.points) || String(entity.action || '').toLowerCase().startsWith('pool:');%0A+     if (poolChoreography) continue;%0A+     const arrived = updateMovement(state, entity, dt);%0A+     if (arrived) resolveArrival(state, entity);%0A+   }%0A+%0A+   enforceGateTraversal(state);%0A+   updateActions(state, dt);%0A+   updateArcadeSystem(state, dt);%0A+   updateBasketballSystem(state, dt);%0A+   updateOffsiteHomeView(state);%0A+   updateCalendarRuntime(state);%0A+   updateAutoHooks(state, dt);%0A+   updateAutonomy(state, dt);%0A+   advanceGameClock(state, dt);%0A+   updateLifeQualitySystem(state);%0A+   updateHouseTidiness(state);%0A+ }%0A+%0A+ function advanceSimulation(state, rawDt) {%0A+   if (state.paused) return;%0A+   const maximumStep = document.hidden ? .2 : .05;%0A+   const maximumCatchup = document.hidden ? 4 : .2;%0A+   let remaining = Math.min(rawDt * state.speed, maximumCatchup);%0A+   let guard = 0;%0A+   while (remaining > .0001 && guard < 40) {%0A+     const step = Math.min(maximumStep, remaining);%0A+     runSimulationStep(state, step);%0A+     remaining -= step;%0A+     guard += 1;%0A+   }%0A+ }%0A+%0A+ function floorSignature(floorId) {%0A+   const floorRooms = floors.find(candidate => candidate.id === floorId)?.rooms || [];%0A+   const floorObjects = objects.filter(object => object.floor === floorId && !object.collisionOnly);%0A+   return `${floorId}|${floorRooms.map(room => `${room.id}:${room.x}:${room.y}:${room.w}:${room.h}`).join(';')}|${floorObjects.map(object => `${object.id}:${object.x}:${object.y}:${object.w}:${object.h}:${object.kind}:${object.hidden ? 1 : 0}`).join(';')}`;%0A+ }%0A+%0A+ function roomAlpha(roomId) {%0A+   if (roomId === 'yard' || roomId === 'front_yard' || roomId === 'garden') return .92;%0A+   if (roomId.includes('pool')) return .9;%0A+   if (roomId.includes('garage') || roomId.includes('driveway') || roomId.includes('road')) return .9;%0A+   return .97;%0A+ }%0A+%0A+ function parseHexColor(value, fallback) {%0A+   if (typeof value !== 'string') return fallback;%0A+   const normalized = value.replace('#', '');%0A+   const parsed = Number.parseInt(normalized, 16);%0A+   return Number.isFinite(parsed) ? parsed : fallback;%0A+ }%0A+%0A+ function loadRefreshStateSafely(state) {%0A+   try {%0A+     const snapshot = loadRefreshState(state);%0A+     if (snapshot?.floor != null) state.floor = snapshot.floor;%0A+     return snapshot;%0A+   } catch (error) {%0A+     clearBadRefreshState();%0A+     console.warn('[Apartment God] Ignored incompatible refresh state.', error);%0A+     return null;%0A+   }%0A+ }%0A+%0A+ function clearBadRefreshState() {%0A+   try { localStorage.removeItem(REFRESH_SAVE_KEY); } catch {}%0A+ }%0A+%0A+ function drawHardBootError(canvas, error) {%0A+   const context = canvas.getContext('2d');%0A+   if (!context) return;%0A+   canvas.width = PLAY_W;%0A+   canvas.height = PLAY_H;%0A+   context.fillStyle = '#171a22';%0A+   context.fillRect(0, 0, PLAY_W, PLAY_H);%0A+   context.fillStyle = '#f1c66a';%0A+   context.font = '900 34px system-ui';%0A+   context.fillText('Phaser Migration 2 could not boot', 42, 84);%0A+   context.fillStyle = '#f0f2f7';%0A+   context.font = '700 18px system-ui';%0A+   context.fillText('The game stopped on a visible error screen instead of showing a blank canvas.', 42, 122);%0A+   context.fillStyle = '#aab2c5';%0A+   context.font = '600 14px system-ui';%0A+   context.fillText(String(error?.message || error || 'Unknown error').slice(0, 150), 42, 160);%0A+ }%0A+%0A%0A ❯ tests/phaser-migration-2-perfecting-runtime.test.js:62:27%0A%0A

::error file=/home/runner/work/apartment-god-prototype/apartment-god-prototype/tests/phaser-migration-2-reference-visuals.test.js,title=tests/phaser-migration-2-reference-visuals.test.js > Phaser Migration 2 reference quality visual catalog > keeps the native Phaser runtime and removes the old broad visual selector,line=33,column=27::AssertionError: expected 'import { PLAY_H, PLAY_W } from \'./co…' to contain 'textureForRoom(room)'%0A%0A- Expected%0A+ Received%0A%0A- textureForRoom(room)%0A+ import { PLAY_H, PLAY_W } from './config.js';%0A+ import { createState, selected } from './state.js';%0A+ import { floors, objects } from './world.js';%0A+ import { createUi } from './ui.js';%0A+ import { updateMovement } from './movement.js';%0A+ import { resolveArrival, updateActions } from './actions.js';%0A+ import { updateAutoHooks } from './autoHooks.js';%0A+ import { updateAutonomy } from './autonomy.js';%0A+ import { updateCalendarRuntime } from './calendarRuntime.js';%0A+ import { updateLifeQualitySystem } from './lifeQualitySystem.js';%0A+ import { installCameraSwipeNavigation, updateCameraTransition } from './cameraNavigation.js';%0A+ import { loadRefreshState, saveRefreshState, updateRefreshAutosave } from './saveSystem.js';%0A+ import { applyRuntimeRegressionGuards } from './runtimeRegressionGuards.js';%0A+ import { updatePoolActivity } from './poolActivitySystem.js';%0A+ import { updateHouseTidiness } from './tidinessSystem.js';%0A+ import { formatTime } from './renderHelpers.js';%0A+ import { advanceGameClock } from './timeSystem.js';%0A+ import { installFrontYardWorld, updateFrontYardEnvironment } from './frontYardDriveway.js';%0A+ import { applyRuntimeObjectCorrections } from './runtimeObjectCorrections.js';%0A+ import { applyMainFloorLayoutPolish } from './mainFloorLayoutPolish.js';%0A+ import { updateArcadeSystem } from './arcadeSystem.js';%0A+ import { updateBasketballSystem } from './basketballSystem.js';%0A+ import { updateOffsiteHomeView } from './offsiteOverlay.js';%0A+ import { captureGateTraversalState, enforceGateTraversal, requestGateForApproachingEntities } from './gateTraversalGuard.js';%0A+ import {%0A+   CHARACTER_ANIMATION_FPS,%0A+   clearCharacterVisuals,%0A+   registerCharacterAnimations,%0A+   syncCharacterVisuals%0A+ } from './phaserCharacterAnimationSystem.js';%0A+ import {%0A+   createNativeGameplayVisuals,%0A+   destroyNativeGameplayVisuals,%0A+   syncNativeGameplayVisuals%0A+ } from './phaserMigration2GameplayVisuals.js';%0A+ import {%0A+   PM2_CHARACTER_SHEETS,%0A+   PM2_OBJECT_TEXTURES,%0A+   PM2_ROOM_TEXTURES%0A+ } from './phaserMigration2VisualCatalog.js';%0A+ import {%0A+   createModernProceduralObject,%0A+   createModernProceduralRoom,%0A+   isModernProceduralVisual,%0A+   refreshModernProceduralObject%0A+ } from './phaserMigration2ModernProceduralRenderer.js';%0A+ import {%0A+   preloadReferenceCompletion,%0A+   createReferenceCompletion,%0A+   syncReferenceCompletion,%0A+   destroyReferenceCompletion%0A+ } from './phaserMigration2ReferenceCompletion.js';%0A+%0A+ installFrontYardWorld();%0A+ applyMainFloorLayoutPolish();%0A+ applyRuntimeObjectCorrections();%0A+%0A+ const REFRESH_SAVE_KEY = 'apartment_god_test_refresh_state_v3';%0A+%0A+ export async function bootPhaserMigration2Game() {%0A+   const canvas = document.getElementById('game');%0A+   if (!canvas) return null;%0A+   try {%0A+     const phaserModule = await import('../vendor/phaser.esm.js').catch(() => import('https://cdn.jsdelivr.net/npm/phaser@3.90.0/dist/phaser.esm.js'));%0A+     const Phaser = phaserModule.default || phaserModule;%0A+     const SceneClass = createApartmentGodNativeScene(Phaser);%0A+     return new Phaser.Game({%0A+       type: Phaser.CANVAS,%0A+       canvas,%0A+       width: PLAY_W,%0A+       height: PLAY_H,%0A+       backgroundColor: '#101722',%0A+       render: { antialias: true, pixelArt: false, transparent: false, clearBeforeRender: true },%0A+       scale: { mode: Phaser.Scale.NONE, autoCenter: Phaser.Scale.NO_CENTER, width: PLAY_W, height: PLAY_H },%0A+       scene: [SceneClass]%0A+     });%0A+   } catch (error) {%0A+     drawHardBootError(canvas, error);%0A+     console.error('[Apartment God] Phaser Migration 2 failed before scene creation.', error);%0A+     return null;%0A+   }%0A+ }%0A+%0A+ function createApartmentGodNativeScene(Phaser) {%0A+   return class ApartmentGodNativeScene extends Phaser.Scene {%0A+     constructor() {%0A+       super('ApartmentGodNativeScene');%0A+       this.currentFloor = null;%0A+       this.floorSignature = '';%0A+       this.frameErrorCount = 0;%0A+       this.runtimeFailed = false;%0A+       this.lastHiddenTick = 0;%0A+       this.hiddenTicker = null;%0A+       this.nativeObjects = [];%0A+       this.pm2ActorVisuals = new Map();%0A+       this.assetFailures = [];%0A+       this.nativeGameplayVisuals = null;%0A+       this.beforeUnloadHandler = null;%0A+       this.visibilityHandler = null;%0A+       this.pointerHandler = null;%0A+     }%0A+%0A+     preload() {%0A+       preloadReferenceCompletion(this);%0A+       this.loadText = this.add.text(28, 28, 'Apartment God Phaser Migration 2 loading full gameplay...', {%0A+         fontFamily: 'system-ui', fontSize: 20, fontStyle: '900', color: '#f1c66a'%0A+       }).setDepth(10000);%0A+%0A+       for (const [key, url] of Object.entries(PM2_ROOM_TEXTURES)) this.load.svg(`pm2-room-${key}`, url, { width: 128, height: 128 });%0A+       for (const [key, url] of Object.entries(PM2_OBJECT_TEXTURES)) this.load.svg(`pm2-object-${key}`, url, { width: 128, height: 128 });%0A+       for (const [key, url] of Object.entries(PM2_CHARACTER_SHEETS)) this.load.svg(`pm2-${key}`, url, { width: 512, height: 512 });%0A+       this.load.on('loaderror', file => {%0A+         const key = file?.key || file?.src || 'unknown asset';%0A+         if (!this.assetFailures.includes(key)) this.assetFailures.push(key);%0A+         console.warn('[Apartment God] Phaser Migration 2 visual asset failed safely:', file?.key, file?.src);%0A+       });%0A+     }%0A+%0A+     create() {%0A+       try {%0A+         this.state = createState();%0A+         this.state.runtimeRenderer = 'phaser-migration-2-native-full-gameplay';%0A+         this.state.assetFailures = [...this.assetFailures];%0A+         loadRefreshStateSafely(this.state);%0A+         sanitizeRuntimeState(this.state);%0A+         applyRuntimeRegressionGuards(this.state);%0A+%0A+         this.roomLayer = this.add.container(0, 0).setDepth(0);%0A+         this.objectLayer = this.add.container(0, 0).setDepth(20);%0A+         this.actorLayer = this.add.container(0, 0).setDepth(60);%0A+         this.fxLayer = this.add.container(0, 0).setDepth(90);%0A+         this.poolGraphics = this.add.graphics();%0A+         this.fxLayer.add(this.poolGraphics);%0A+         this.nativeGameplayVisuals = createNativeGameplayVisuals(this, this.fxLayer);%0A+         createReferenceCompletion(this);%0A+         registerCharacterAnimations(this);%0A+%0A+         this.statusText = this.add.text(12, 12, '', {%0A+           fontFamily: 'system-ui', fontSize: 14, fontStyle: '900', color: '#f8fbff', backgroundColor: 'rgba(7,10,16,.55)', padding: { x: 8, y: 5 }%0A+         }).setDepth(1000);%0A+         this.runtimeText = this.add.text(12, PLAY_H - 25, runtimeLabel(this.assetFailures), {%0A+           fontFamily: 'system-ui', fontSize: 11, fontStyle: '900', color: '#f1c66a', backgroundColor: 'rgba(7,10,16,.45)', padding: { x: 7, y: 3 }%0A+         }).setDepth(1000);%0A+%0A+         installCameraSwipeNavigation(this.state, this.game.canvas);%0A+         this.ui = createUi(this.state, this.game.canvas, { externalInput: true });%0A+         this.pointerHandler = pointer => this.handleGamePointer(pointer);%0A+         this.input.on('pointerdown', this.pointerHandler);%0A+%0A+         this.beforeUnloadHandler = () => {%0A+           if (!this.runtimeFailed && this.state) saveRefreshState(this.state);%0A+         };%0A+         this.visibilityHandler = () => {%0A+           if (!this.state || this.runtimeFailed) return;%0A+           this.state.backgroundMode = document.hidden;%0A+           if (!document.hidden) this.state.saveStatus = { message: 'Returned from background' };%0A+         };%0A+         window.addEventListener('beforeunload', this.beforeUnloadHandler);%0A+         document.addEventListener('visibilitychange', this.visibilityHandler);%0A+%0A+         this.lastHiddenTick = performance.now();%0A+         this.hiddenTicker = window.setInterval(() => this.hiddenTick(), 1000);%0A+         this.events.once('shutdown', () => this.shutdownRuntime());%0A+         this.events.once('destroy', () => this.shutdownRuntime());%0A+%0A+         this.registry.set('apartmentGodRuntime', 'phaser-migration-2-native-full-main-gameplay');%0A+         this.registry.set('apartmentGodCharacterFps', CHARACTER_ANIMATION_FPS);%0A+         this.registry.set('apartmentGodAssetFallbackCount', this.assetFailures.length);%0A+         if (this.assetFailures.length) this.state.saveStatus = { message: `${this.assetFailures.length} visual asset fallback(s) active` };%0A+         this.loadText?.destroy();%0A+         this.renderNativeFrame();%0A+         this.ui?.renderHud();%0A+       } catch (error) {%0A+         this.recoverFrame(error, true);%0A+       }%0A+     }%0A+%0A+     update(_time, deltaMs) {%0A+       if (this.runtimeFailed || !this.state) return;%0A+       try {%0A+         sanitizeRuntimeState(this.state);%0A+         applyRuntimeRegressionGuards(this.state);%0A+         const rawElapsed = Math.max(0, deltaMs / 1000);%0A+         const rawDt = Math.min(document.hidden ? 4 : .2, rawElapsed);%0A+         const cameraDt = Math.min(.05, rawElapsed);%0A+         updateCameraTransition(this.state, cameraDt);%0A+         if (!document.hidden) advanceSimulation(this.state, rawDt);%0A+         updateRefreshAutosave(this.state, cameraDt);%0A+         if (this.state.skipRecap?.visibleT > 0) this.state.skipRecap.visibleT -= cameraDt;%0A+         this.renderNativeFrame();%0A+         this.ui?.renderHud();%0A+       } catch (error) {%0A+         this.recoverFrame(error, false);%0A+       }%0A+     }%0A+%0A+     handleGamePointer(pointer) {%0A+       if (this.runtimeFailed || !this.ui?.handleCanvasPoint) return;%0A+       const bounds = this.game.canvas.getBoundingClientRect();%0A+       const event = pointer.event;%0A+       const menuX = event?.clientX == null ? pointer.x : event.clientX - bounds.left;%0A+       const menuY = event?.clientY == null ? pointer.y : event.clientY - bounds.top;%0A+       this.ui.handleCanvasPoint(pointer.x, pointer.y, menuX, menuY);%0A+     }%0A+%0A+     hiddenTick() {%0A+       if (this.runtimeFailed || !this.state || !document.hidden || this.state.paused) return;%0A+       const now = performance.now();%0A+       const elapsed = Math.min(4, Math.max(0, (now - this.lastHiddenTick) / 1000));%0A+       this.lastHiddenTick = now;%0A+       try {%0A+         sanitizeRuntimeState(this.state);%0A+         applyRuntimeRegressionGuards(this.state);%0A+         advanceSimulation(this.state, elapsed);%0A+         updateRefreshAutosave(this.state, Math.min(1, elapsed));%0A+       } catch (error) {%0A+         this.recoverFrame(error, false);%0A+       }%0A+     }%0A+%0A+     renderNativeFrame() {%0A+       if (this.runtimeFailed || !this.state) return;%0A+       const signature = floorSignature(this.state.floor);%0A+       if (this.currentFloor !== this.state.floor || this.floorSignature !== signature) this.rebuildFloor(signature);%0A+       this.refreshObjectStates();%0A+       syncCharacterVisuals(this, this.state, this.actorLayer);%0A+       this.refreshPoolFx();%0A+       syncReferenceCompletion(this);%0A+       syncNativeGameplayVisuals(this, this.state, this.nativeGameplayVisuals);%0A+       const actor = selected(this.state);%0A+       const tidy = Number.isFinite(this.state.tidiness?.score) ? ` tidy ${Math.round(this.state.tidiness.score)}%25` : '';%0A+       const floor = floors.find(candidate => candidate.id === this.state.floor);%0A+       this.statusText?.setText(`${formatTime(this.state.time)}   $${Math.round(this.state.money ?? 0)}   ${this.state.autonomyMode}${tidy}   ${floor?.name || this.state.floor}   ${actor?.name || ''}: ${actor?.action || 'Idle'}`);%0A+       this.statusText?.setVisible(true);%0A+       this.runtimeText?.setVisible(true);%0A+     }%0A+%0A+     rebuildFloor(signature = floorSignature(this.state.floor)) {%0A+       if (this.runtimeFailed || !this.roomLayer || !this.objectLayer) return;%0A+       this.currentFloor = this.state.floor;%0A+       this.floorSignature = signature;%0A+       this.roomLayer.removeAll(true);%0A+       this.objectLayer.removeAll(true);%0A+       this.nativeObjects = [];%0A+       const floor = floors.find(candidate => candidate.id === this.state.floor);%0A+       if (!floor) return;%0A+%0A+       for (const room of floor.rooms || []) {%0A+         const panel = createModernProceduralRoom(this, room, floor.id, this.state);%0A+         this.roomLayer.add(panel);%0A+         const label = this.add.text(room.x + 16, room.y + 14, room.name, {%0A+           fontFamily: 'system-ui', fontSize: 11, fontStyle: '900', color: '#3f352d', backgroundColor: 'rgba(247,242,233,.72)', padding: { x: 5, y: 3 }%0A+         });%0A+         label.setVisible(Boolean(this.state.debugVisualLabels));%0A+         label.setDepth(room.y - 850);%0A+         this.roomLayer.add(label);%0A+       }%0A+%0A+       const floorObjects = objects%0A+         .filter(candidate => candidate.floor === this.state.floor)%0A+         .sort((a, b) => (a.y + a.h) - (b.y + b.h));%0A+       for (const object of floorObjects) {%0A+         const visual = createModernProceduralObject(this, object, this.state);%0A+         this.objectLayer.add(visual);%0A+         this.nativeObjects.push(visual);%0A+       }%0A+       this.refreshObjectStates();%0A+     }%0A+%0A+     refreshObjectStates() {%0A+       if (this.runtimeFailed) return;%0A+       for (const sprite of this.nativeObjects) {%0A+         const object = sprite.pm2Object;%0A+         if (!object) continue;%0A+         const visible = object.floor === this.state.floor && !object.hidden && !object.collisionOnly;%0A+         sprite.setVisible(visible);%0A+         if (!visible) continue;%0A+         if (isModernProceduralVisual(sprite)) {%0A+           sprite.setPosition(object.x + object.w / 2, object.y + object.h / 2);%0A+           sprite.setDepth(object.y + object.h);%0A+           sprite.setRotation(0);%0A+           sprite.setAlpha(1);%0A+           refreshModernProceduralObject(sprite, this.state);%0A+           continue;%0A+         }%0A+         sprite.setPosition(object.x + object.w / 2, object.y + object.h / 2);%0A+         sprite.setDisplaySize(Math.max(18, object.w), Math.max(18, object.h));%0A+         sprite.setDepth(object.y + object.h);%0A+         sprite.setRotation(Number(object.renderAngle || 0));%0A+         sprite.clearTint();%0A+         sprite.setAlpha(object.kind === 'light' ? .5 : 1);%0A+%0A+         if (object.kind === 'tv') sprite.setTint(this.state.tv?.on ? 0x74e6ff : 0x5d6876);%0A+         if (['shower','bathtub','dog_bath'].includes(object.kind)) {%0A+           const active = this.state.entities?.some(entity => entity.showerObjectId === object.id && entity.actionT > 0)%0A+             || this.state.entities?.some(entity => entity.currentActionId === 'wash_dog' && entity.actionT > 0 && entity.floor === object.floor);%0A+           if (active) sprite.setTint(0x9feaff);%0A+         }%0A+         if (object.kind === 'toilet') {%0A+           const active = this.state.entities?.some(entity => entity.toiletObjectId === object.id && entity.actionT > 0);%0A+           if (active) sprite.setTint(0xf1c66a);%0A+         }%0A+         if (object.kind === 'basketball_court') sprite.setAlpha(.32);%0A+         if (object.id?.includes('garage_door') || String(object.label || '').toLowerCase().includes('garage door')) {%0A+           sprite.setAlpha(this.state.objectState?.garageDoorOpen ? .35 : 1);%0A+           if (this.state.objectState?.garageDoorOpen) sprite.setTint(0x74e6ff);%0A+         }%0A+       }%0A+     }%0A+%0A+     refreshPoolFx() {%0A+       const graphics = this.poolGraphics;%0A+       if (!graphics || this.runtimeFailed) return;%0A+       graphics.clear();%0A+       const game = this.state.poolGame;%0A+       if (!game || game.floor !== this.state.floor || !Array.isArray(game.balls)) return;%0A+%0A+       if (game.cueLine?.t > 0) {%0A+         graphics.lineStyle(2, 0xf1c66a, Math.min(1, game.cueLine.t));%0A+         graphics.lineBetween(game.cueLine.x1, game.cueLine.y1, game.cueLine.x2, game.cueLine.y2);%0A+       }%0A+       if (game.cueThrust?.t > 0) {%0A+         graphics.lineStyle(4, 0xc99d62, Math.min(1, game.cueThrust.t * 2));%0A+         graphics.lineBetween(game.cueThrust.x1, game.cueThrust.y1, game.cueThrust.x2, game.cueThrust.y2);%0A+       }%0A+       for (const ball of game.balls) {%0A+         if (ball.pocketed) continue;%0A+         graphics.fillStyle(parseHexColor(ball.fill, 0xf8fbff), 1);%0A+         graphics.fillCircle(ball.x, ball.y, 7);%0A+         graphics.lineStyle(1.5, 0x111820, 1);%0A+         graphics.strokeCircle(ball.x, ball.y, 7);%0A+       }%0A+     }%0A+%0A+     shutdownRuntime() {%0A+       if (this.hiddenTicker) window.clearInterval(this.hiddenTicker);%0A+       this.hiddenTicker = null;%0A+       if (this.beforeUnloadHandler) window.removeEventListener('beforeunload', this.beforeUnloadHandler);%0A+       if (this.visibilityHandler) document.removeEventListener('visibilitychange', this.visibilityHandler);%0A+       if (this.pointerHandler) this.input?.off?.('pointerdown', this.pointerHandler);%0A+       this.beforeUnloadHandler = null;%0A+       this.visibilityHandler = null;%0A+       this.pointerHandler = null;%0A+       destroyNativeGameplayVisuals(this.nativeGameplayVisuals);%0A+       this.nativeGameplayVisuals = null;%0A+       destroyReferenceCompletion(this);%0A+       clearCharacterVisuals(this);%0A+     }%0A+%0A+     recoverFrame(error, boot = false) {%0A+       if (this.runtimeFailed) return;%0A+       this.runtimeFailed = true;%0A+       this.frameErrorCount += 1;%0A+       console.error('[Apartment God] Phaser Migration 2 entered terminal recovery instead of blanking.', error);%0A+       clearBadRefreshState();%0A+%0A+       if (this.hiddenTicker) window.clearInterval(this.hiddenTicker);%0A+       this.hiddenTicker = null;%0A+       if (this.pointerHandler) this.input?.off?.('pointerdown', this.pointerHandler);%0A+       if (this.input) this.input.enabled = false;%0A+%0A+       if (this.state) {%0A+         try { sanitizeRuntimeState(this.state); } catch {}%0A+         for (const entity of this.state.entities || []) {%0A+           entity.path = [];%0A+           entity.poolRoute = null;%0A+           entity.target = null;%0A+           entity.pending = null;%0A+           entity.pose = 'stand';%0A+           entity.blockedT = 0;%0A+           entity.recoveryCount = 0;%0A+         }%0A+         this.state.saveStatus = { message: 'Phaser Migration 2 stopped safely after a runtime error' };%0A+         this.state.paused = true;%0A+       }%0A+%0A+       try { destroyNativeGameplayVisuals(this.nativeGameplayVisuals); } catch {}%0A+       this.nativeGameplayVisuals = null;%0A+       try { destroyReferenceCompletion(this); } catch {}%0A+       try { clearCharacterVisuals(this); } catch {}%0A+       try { this.children.removeAll(true); } catch {}%0A+%0A+       this.cameras?.main?.setBackgroundColor?.('#171a22');%0A+       this.add.rectangle(0, 0, PLAY_W, PLAY_H, 0x171a22).setOrigin(0, 0);%0A+       this.add.text(32, 64, boot ? 'Phaser Migration 2 boot recovery screen' : 'Phaser Migration 2 runtime recovery screen', {%0A+         fontFamily: 'system-ui', fontSize: 28, fontStyle: '900', color: '#f1c66a'%0A+       });%0A+       this.add.text(32, 108, 'The game stopped safely. The error screen will remain stable instead of looping or blanking.', {%0A+         fontFamily: 'system-ui', fontSize: 17, fontStyle: '700', color: '#f0f2f7', wordWrap: { width: PLAY_W - 64 }%0A+       });%0A+       this.add.text(32, 158, String(error?.message || error || 'Unknown error').slice(0, 220), {%0A+         fontFamily: 'system-ui', fontSize: 14, color: '#aab2c5', wordWrap: { width: PLAY_W - 64 }%0A+       });%0A+       this.registry.set('apartmentGodRuntimeFailed', true);%0A+       this.registry.set('apartmentGodRuntimeError', String(error?.message || error || 'Unknown error'));%0A+       this.scene.pause();%0A+     }%0A+   };%0A+ }%0A+%0A+ function runtimeLabel(assetFailures) {%0A+   const fallback = assetFailures.length ? ` | ${assetFailures.length} visual fallback${assetFailures.length === 1 ? '' : 's'}` : '';%0A+   return `PHASER MIGRATION 2 | native world | full gameplay | ${CHARACTER_ANIMATION_FPS} FPS${fallback}`;%0A+ }%0A+%0A+ function sanitizeRuntimeState(state) {%0A+   state.entities = Array.isArray(state.entities) ? state.entities : [];%0A+   state.speed = Number.isFinite(state.speed) && state.speed > 0 ? state.speed : 1;%0A+   state.time = Number.isFinite(state.time) ? state.time : 0;%0A+   state.floor = Number.isInteger(state.floor) ? state.floor : 0;%0A+   state.objectState ??= {};%0A+   state.roomLights ??= {};%0A+   state.tidiness ??= { rooms: {}, score: 100, activityMultiplier: 1.2 };%0A+   state.tidiness.rooms ??= {};%0A+   state.notifications = Array.isArray(state.notifications) ? state.notifications : [];%0A+   state.lifeControl ??= { mode: 'semi_auto', pendingChoices: [] };%0A+   state.lifeQuality ??= { lastMonthIndex: null, lastYearIndex: null, reviews: [], yearReviews: [] };%0A+   state.frontGate ??= { open: 0, requested: false };%0A+   applyMainFloorLayoutPolish(state);%0A+   applyRuntimeObjectCorrections();%0A+   for (const entity of state.entities) {%0A+     entity.path = Array.isArray(entity.path) ? entity.path : [];%0A+     entity.needs ??= {};%0A+     entity.skills ??= {};%0A+     entity.pose ||= 'stand';%0A+     entity.action ||= 'Idle';%0A+     entity.floor = Number.isInteger(entity.floor) ? entity.floor : 0;%0A+     entity.x = Number.isFinite(entity.x) ? entity.x : 120;%0A+     entity.y = Number.isFinite(entity.y) ? entity.y : 120;%0A+     cleanupStaleActorState(entity);%0A+   }%0A+   updateHouseTidiness(state);%0A+ }%0A+%0A+ function cleanupStaleActorState(entity) {%0A+   const hasPath = Array.isArray(entity.path) && entity.path.length > 0;%0A+   const hasPoolRoute = Array.isArray(entity.poolRoute?.points) && entity.poolRoute.points.length > 0;%0A+   const hasTarget = Boolean(entity.target || entity.pending);%0A+   const hasTimer = Number(entity.actionT || 0) > 0;%0A+   if (hasPath || hasPoolRoute || hasTarget || hasTimer || entity.hidden) return;%0A+   const action = String(entity.action || '').toLowerCase();%0A+   if (action === 'recovered' || action === 'runtime recovered' || action === 'walking' || action === 'running' || action.startsWith('going to ')) {%0A+     entity.action = 'Idle';%0A+     entity.pose = 'stand';%0A+     entity.blockedT = 0;%0A+     entity.recoveryCount = 0;%0A+   }%0A+ }%0A+%0A+ function runSimulationStep(state, dt) {%0A+   if (dt <= 0) return;%0A+   applyMainFloorLayoutPolish(state);%0A+   applyRuntimeObjectCorrections();%0A+   updateFrontYardEnvironment(state, dt);%0A+   requestGateForApproachingEntities(state, dt);%0A+   updateHouseTidiness(state);%0A+   updatePoolActivity(state, dt);%0A+   captureGateTraversalState(state);%0A+%0A+   for (const entity of state.entities) {%0A+     const poolChoreography = Array.isArray(entity.poolRoute?.points) || String(entity.action || '').toLowerCase().startsWith('pool:');%0A+     if (poolChoreography) continue;%0A+     const arrived = updateMovement(state, entity, dt);%0A+     if (arrived) resolveArrival(state, entity);%0A+   }%0A+%0A+   enforceGateTraversal(state);%0A+   updateActions(state, dt);%0A+   updateArcadeSystem(state, dt);%0A+   updateBasketballSystem(state, dt);%0A+   updateOffsiteHomeView(state);%0A+   updateCalendarRuntime(state);%0A+   updateAutoHooks(state, dt);%0A+   updateAutonomy(state, dt);%0A+   advanceGameClock(state, dt);%0A+   updateLifeQualitySystem(state);%0A+   updateHouseTidiness(state);%0A+ }%0A+%0A+ function advanceSimulation(state, rawDt) {%0A+   if (state.paused) return;%0A+   const maximumStep = document.hidden ? .2 : .05;%0A+   const maximumCatchup = document.hidden ? 4 : .2;%0A+   let remaining = Math.min(rawDt * state.speed, maximumCatchup);%0A+   let guard = 0;%0A+   while (remaining > .0001 && guard < 40) {%0A+     const step = Math.min(maximumStep, remaining);%0A+     runSimulationStep(state, step);%0A+     remaining -= step;%0A+     guard += 1;%0A+   }%0A+ }%0A+%0A+ function floorSignature(floorId) {%0A+   const floorRooms = floors.find(candidate => candidate.id === floorId)?.rooms || [];%0A+   const floorObjects = objects.filter(object => object.floor === floorId && !object.collisionOnly);%0A+   return `${floorId}|${floorRooms.map(room => `${room.id}:${room.x}:${room.y}:${room.w}:${room.h}`).join(';')}|${floorObjects.map(object => `${object.id}:${object.x}:${object.y}:${object.w}:${object.h}:${object.kind}:${object.hidden ? 1 : 0}`).join(';')}`;%0A+ }%0A+%0A+ function roomAlpha(roomId) {%0A+   if (roomId === 'yard' || roomId === 'front_yard' || roomId === 'garden') return .92;%0A+   if (roomId.includes('pool')) return .9;%0A+   if (roomId.includes('garage') || roomId.includes('driveway') || roomId.includes('road')) return .9;%0A+   return .97;%0A+ }%0A+%0A+ function parseHexColor(value, fallback) {%0A+   if (typeof value !== 'string') return fallback;%0A+   const normalized = value.replace('#', '');%0A+   const parsed = Number.parseInt(normalized, 16);%0A+   return Number.isFinite(parsed) ? parsed : fallback;%0A+ }%0A+%0A+ function loadRefreshStateSafely(state) {%0A+   try {%0A+     const snapshot = loadRefreshState(state);%0A+     if (snapshot?.floor != null) state.floor = snapshot.floor;%0A+     return snapshot;%0A+   } catch (error) {%0A+     clearBadRefreshState();%0A+     console.warn('[Apartment God] Ignored incompatible refresh state.', error);%0A+     return null;%0A+   }%0A+ }%0A+%0A+ function clearBadRefreshState() {%0A+   try { localStorage.removeItem(REFRESH_SAVE_KEY); } catch {}%0A+ }%0A+%0A+ function drawHardBootError(canvas, error) {%0A+   const context = canvas.getContext('2d');%0A+   if (!context) return;%0A+   canvas.width = PLAY_W;%0A+   canvas.height = PLAY_H;%0A+   context.fillStyle = '#171a22';%0A+   context.fillRect(0, 0, PLAY_W, PLAY_H);%0A+   context.fillStyle = '#f1c66a';%0A+   context.font = '900 34px system-ui';%0A+   context.fillText('Phaser Migration 2 could not boot', 42, 84);%0A+   context.fillStyle = '#f0f2f7';%0A+   context.font = '700 18px system-ui';%0A+   context.fillText('The game stopped on a visible error screen instead of showing a blank canvas.', 42, 122);%0A+   context.fillStyle = '#aab2c5';%0A+   context.font = '600 14px system-ui';%0A+   context.fillText(String(error?.message || error || 'Unknown error').slice(0, 150), 42, 160);%0A+ }%0A+%0A%0A ❯ tests/phaser-migration-2-reference-visuals.test.js:33:27%0A%0A
```

## Build log
```text
Not run.
```
